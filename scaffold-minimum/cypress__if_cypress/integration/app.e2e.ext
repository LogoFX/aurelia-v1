/// <reference types="Cypress" />

context('Aurelia skeleton app', () => {
  beforeEach(() => {
    cy.visit('/');
  });

  it('should display greeting', () => {
    cy.get('h2').contains('Hello World!');
  });
});
